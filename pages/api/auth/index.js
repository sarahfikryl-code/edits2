// /pages/api/auth/index.js
// Placeholder for authentication API routes. Actual logic will be added in subroutes (login, me, assistants, etc.)

export default function handler(req, res) {
  res.status(404).json({ error: 'Not found' });
} 